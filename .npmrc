# GitHub Packages: 스코프는 저장소 소유자(JunhaDex)와 일치해야 한다.
@junhadex:registry=https://npm.pkg.github.com
# 인증 토큰은 이 파일에 두지 않는다.
# 로컬: ~/.npmrc 에 "//npm.pkg.github.com/:_authToken=<PAT(write:packages)>" 추가
# CI:   NODE_AUTH_TOKEN 환경변수 + actions/setup-node 의 registry-url 옵션 사용
